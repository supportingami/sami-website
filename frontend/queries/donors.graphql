# Use GraphQL to extract data from the database
# This is comparable to SQL
# `SELECT Name, Email from members`
query donors {
  # table to query
  donors(sort: "SortOrder:ASC") {
    # return from query data
    data {
      # primary key to use
      id
      # what fields to return
      attributes {
        Name
        Logo {
          data {
            id
            attributes {
              url
              name
              size
            }
          }
        }
      }
    }
    # return from query meta
    meta {
      pagination {
        page
        pageSize
        total
        pageCount
      }
    }
  }
}
