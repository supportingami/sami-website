# Write your query or mutation here
query partners {
  partners_connection(sort: "SortOrder:ASC") {
    nodes {
      documentId
      Name
      Logo {
        documentId
        name
        url
        size
      }
    }
  }
}
