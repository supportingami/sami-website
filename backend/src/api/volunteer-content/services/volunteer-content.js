"use strict";

/**
 * volunteer-content service.
 */

const { createCoreService } = require("@strapi/strapi").factories;

module.exports = createCoreService("api::volunteer-content.volunteer-content");
